# Runtime state from stack.sh (pids, logs) — not source.
.stack/
# macOS
.DS_Store
**/.DS_Store

# The new agents monorepo is a nested git repo with no remote yet. Track it as a
# submodule once it is pushed; until then keep it out of the stack repo's status.
qms-agents/
